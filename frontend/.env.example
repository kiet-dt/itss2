# Cách 1 (khuyên dùng): sửa domain Railway trong frontend/vercel.json → giữ /api qua proxy Vercel
#
# Cách 2: set biến này trên Vercel (Settings → Environment Variables), không cần sửa vercel.json
# VITE_API_BASE=https://your-app.up.railway.app/api
